// 🚀 업데이트 소식 — 베타 테스터가 OTA 업데이트를 인지하도록 앱 안에서 1회 안내.
//   OTA 는 조용히(다음 실행 때) 적용돼 "업데이트된 줄 모른다" 문제 → 태그 바뀔 때만 1회 모달.
//   iOS/안드 공통 유일 채널(외부 이메일·카톡 없음)이라 앱 내 노출이 유일한 방법.
//
// 사용법: 알리고 싶은 릴리스마다 `tag` 를 바꾸고 `lines` 갱신 (OTA 낼 때 같이).
//   같은 tag 는 사용자당 1회만 노출(SecureStore 저장). 정식 출시 땐 tag='' 로 두면 자동 off.
export const RELEASE_NOTE: {
  tag: string;            // '' 이면 모달 비활성
  title: string;
  lines: string[];
  tip: string;
} = {
  tag: 'v2.31-2026-08-19',
  title: '이번 업데이트 소식',
  lines: [
    '내 정보에 "하루 기준선" 이 생겼어요 — 해외에 있어도 그곳의 자정 기준으로 하루를 세요',
    '지금까지는 한국 시간 자정이 기준이라, 해외에서는 저녁에 한 인증이 다음 날로 넘어가 연속이 끊기곤 했어요',
    '기기 위치와 기준선이 다르면 내 정보에서 바꾸라고 알려드려요 (하루에 한 번 바꿀 수 있어요)',
    '사진 인증·기록 저장이 가끔 실패하던 문제를 고쳤어요 — 잠깐 끊겨도 앱이 알아서 다시 시도해요',
  ],
  // 강제 종료 2회 안내 — OTA 는 다음 실행 때 적용(콜드스타트 2번이면 확실히 최신)
  tip: '새 업데이트가 안 보이면: 앱을 완전히 종료(화면에서 위로 쓸어올려 닫기)했다 다시 켜기를 2번 하면 최신으로 반영돼요.',
};
